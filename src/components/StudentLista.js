import React, { Component } from 'react';

class StudentLista extends Component {
    render() {
        return (
            <section id="lista">
                Tässä on haettu lista.
            </section>
        );
    }
}

export default StudentLista;

